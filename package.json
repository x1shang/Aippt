{
  "name": "aippt",
  "productName": "AIPPT",
  "version": "2.2.0",
  "description": "现代化的 AI PPT 生成器：公式/图片/表格/定理/代码高亮原样渲染，Beamer 风格渐进显示，自动编号与交叉引用，支持任意 OpenAI 兼容 API",
  "main": "main/main.js",
  "author": "AIPPT Team",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "dist": "electron-builder --win portable --publish never",
    "test": "node test/run-core-tests.js",
    "icon": "powershell -ExecutionPolicy Bypass -File assets/make-icon.ps1"
  },
  "dependencies": {
    "highlight.js": "^11.12.0",
    "katex": "^0.18.7",
    "pptxgenjs": "^3.12.0"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.12"
  },
  "build": {
    "appId": "com.aippt.app",
    "productName": "AIPPT",
    "asar": true,
    "directories": {
      "output": "dist"
    },
    "files": [
      "main/**/*",
      "renderer/**/*",
      "shared/**/*",
      "assets/**/*",
      "package.json"
    ],
    "win": {
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        }
      ],
      "icon": "assets/icon.ico",
      "artifactName": "AIPPT-${version}-portable.${ext}",
      "requestedExecutionLevel": "asInvoker"
    },
    "portable": {
      "artifactName": "AIPPT-${version}-portable.${ext}"
    }
  }
}
